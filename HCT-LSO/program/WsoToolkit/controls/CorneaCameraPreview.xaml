<UserControl x:Class="WsoToolkit.controls.CorneaCameraPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WsoToolkit.controls"
             mc:Ignorable="d" 
             d:DesignWidth="328" d:DesignHeight="280" Unloaded="UserControl_Unloaded">
    <Grid Background="White">
        <Grid Margin="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="32"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Grid.Column="0" Background="LightGray">
                <Image x:Name="imageViewport" Style="{StaticResource ImageStyle}" Stretch="Uniform"/>
            </Grid>
            <Grid Grid.Row="1" Grid.Column="0" Margin="8, 4, 8, 4">
                <Label x:Name="lblStatus" Content="Cornea Camera" Style="{StaticResource LabelStyle}"/>
                <CheckBox x:Name="checkSwitch" Content="ON" Style="{StaticResource CheckBoxStyle}" Click="CheckSwitch_Click" HorizontalAlignment="Right" Margin="0,0,12,0"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
