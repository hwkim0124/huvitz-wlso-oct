<Window x:Class="WsoToolkit.CaptureROISettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WsoToolkit"
        mc:Ignorable="d"
        Title="Capture ROI Setting" Height="450" Width="500" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <DataGrid x:Name="myDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="False" Margin="10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Frame Number" Binding="{Binding FrameNumber}" Width="100" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Start Y" Binding="{Binding StartY}" Width="60"/>
                    <DataGridTextColumn Header="End Y" Binding="{Binding EndY}" Width="60"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" Grid.ColumnSpan="3">
                <Label x:Name="myLbFrameSize" Content="Frame Size : " Style="{StaticResource LabelStyle}" HorizontalAlignment="Left" Margin="8,0,0,0"/>
            </Grid>
            <Grid Grid.Column="3">
                <Button x:Name="mybtApply" Content="Apply" Style="{StaticResource ButtonS1Style}" Click="mybtApply_Click"/>
            </Grid>
            <Grid Grid.Column="4">
                <Button x:Name="mybtClose" Content="Close" Style="{StaticResource ButtonS1Style}" Click="mybtClose_Click"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
